// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#0d47a1', // Adjust to your desired primary color
        },
        secondary: {
            main: '#ffcc00', // Adjust to your desired secondary color
        },
    },
});

export default theme;
